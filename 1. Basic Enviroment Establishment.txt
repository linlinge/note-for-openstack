4. Network Time Protocol
The function of NTP is to synchronize clock on net.



4.4 Verify Procedure
	ps -ef | grep ntpd			其中|用的是管道命令，可以将ps -ef的结果传给grep ntpd进行筛选
	
5. database of mariadb( controller)
